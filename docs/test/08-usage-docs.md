# T09 — usage and API docs

1. Open **Usage & cost** and verify all cards, charts, filters, and empty/error states render without layout overflow.
2. Open **API docs** and verify the endpoint examples, copy buttons, and code blocks.
3. Click every copy button and paste into a text editor.
4. Resize from desktop to mobile and refresh.

**Pass:** controls are keyboard reachable, copy actions work, and no credential is embedded in displayed examples. Current usage cards are fixture data unless a live endpoint is explicitly labeled.
