# T16 — navigation, locale, accessibility, and responsive layout

1. From `/en/login`, navigate through every workspace and operator link.
2. Repeat the auth pages and core console in `/id`.
3. Resize to 375px, 768px, and desktop widths.
4. Navigate using keyboard only: Tab, Shift+Tab, Enter, Escape, and arrow keys in menus/dialogs.
5. Check focus visibility, labels, dialog escape, color contrast, and no horizontal scroll.
6. Open a missing route and a malformed locale/tenant route.

**Pass:** links resolve, active navigation is correct, both locales render, mobile layout is usable, keyboard focus is visible, and missing routes show a controlled not-found/redirect state.
